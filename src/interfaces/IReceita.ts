// Declaração de uma interface chamada IReceita
export default interface IReceita {
  // Propriedade que representa o nome da receita (assumindo que é uma string)
  nome: string;

  // Propriedade que representa uma lista de ingredientes (assumindo que são strings)
  ingredientes: string[];

  // Propriedade que representa o caminho para a imagem da receita (assumindo que é uma string)
  imagem: string;
}
