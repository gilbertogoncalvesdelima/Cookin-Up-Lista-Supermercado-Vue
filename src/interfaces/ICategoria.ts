// Declaração de uma interface chamada ICategoria
export default interface ICategoria {
  // Propriedade que representa o nome da categoria (assumindo que é uma string)
  nome: string;

  // Propriedade que representa uma lista de ingredientes (assumindo que são strings)
  ingredientes: string[];

  // Propriedade que representa o caminho para a imagem da categoria (assumindo que é uma string)
  imagem: string;
}
